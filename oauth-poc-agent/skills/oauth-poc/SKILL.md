---
name: oauth-poc
description: Explain and validate the mock OAuth proof-of-concept flow.
---

# OAuth POC

Use the mock OAuth app artifact when the user asks to connect an account.

After the flow completes, use `oauth_status` to confirm whether the mock account is connected.
