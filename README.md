# IQuly agent examples

This folder is the workspace home for public agent examples.

Use it for agent packages that should be visible to external authors and that
demonstrate how to build against the public starter package and CLI.

Current public agent packages:

- `weather-agent`

Keep private, dogfood-only agents in `private-agents/` instead.
